import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1e4660',
    },
    text: {
      primary: '#333',
    }
  },
});

export default theme;
